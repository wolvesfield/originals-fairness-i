## 2024-05-24 - [Optimize HMAC-SHA256 calculations in `crypto-js`]
**Learning:** [When dealing with hot-path algorithms that repeatedly use `CryptoJS.HmacSHA256(message, key).toString(CryptoJS.enc.Hex)`, we can obtain a dramatic speed up (~60% faster) by avoiding string conversions entirely. Since Javascript integers securely scale up to 53 bits without floating-point precision loss, we can directly bit-shift values directly from the internal `hash.words` array `(hash.words[0] >>> 0) / 4294967296`. Additionally, we can cache the HMAC instance avoiding reallocation overhead (`HMAC.create(...)`, `.reset()`, `.update(...)`, `.finalize()`).]
**Action:** [When seeing repeated hashing in a tight loop in Javascript `crypto-js`, apply the `.reset()` / `.update()` object caching pattern and extract variables from the internal `hash.words[X]` directly to bypass the slow toString() hex extraction parsing.]
