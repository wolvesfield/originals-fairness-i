name: Security Scanning

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  # Added a dummy job so the workflow is valid
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Security scanning requires GitHub Advanced Security, which is not currently enabled."
  # codeql and dependency-review require GitHub Advanced Security to be enabled
  # at the repository level, which is not currently the case.
  # Disabled for now to prevent CI failures.
  # codeql:
  #   runs-on: ubuntu-latest
  #   continue-on-error: true
  #   permissions:
  #     security-events: write
  #     actions: read
  #     contents: read
  #   strategy:
  #     matrix:
  #       language: [javascript-typescript]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: github/codeql-action/init@v3
  #       with:
  #         languages: ${{ matrix.language }}
  #     - uses: github/codeql-action/analyze@v3
  #
  # dependency-review:
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'pull_request'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/dependency-review-action@v4
